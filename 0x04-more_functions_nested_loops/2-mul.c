#include "main.h"

/**
 * mul - entry point
 * @a: the integer argument a
 * @b: the integer argument b
 * Return: 0 upon successful execution
 */

int mul(int a, int b)
{
	int mul_res = a * b;
	_putchar(mul_res);
	return (0);
}
